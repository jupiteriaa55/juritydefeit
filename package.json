{
  "name": "merry-gravedigger",
  "version": "0.1.0",
  "description": "Весёлый Могильщик — 2.5D мультяшный тайм-менеджер симулятор кладбища (Three.js + PWA + Android).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "lint": "echo 'no linter configured'",
    "check": "node -e \"import('./src/main.js').then(()=>console.log('module loads'))\" || true",
    "icons": "node scripts/gen-icons.mjs",
    "cap:sync": "cap sync",
    "cap:android": "cap add android || true && cap sync android",
    "android:build": "npm run build && cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vite-plugin-pwa": "^0.20.5",
    "@capacitor/cli": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/android": "^6.1.2"
  }
}
